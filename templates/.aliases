# Dir inspection
alias ls='eza'
alias l='eza'
alias ll='eza -alhF'
alias la='eza -a'
alias lt='eza -T --level=3'
# File inspection
alias cat=bat
alias open=xdg-open
# tmux
alias mux=tmuxinator

# clis
alias c=cntb
alias k=kubectl
alias koidc=kubectl-oidc_login
#alias docker=podman
alias cht='cht.sh'
alias chts='cht.sh --shell'


# Apps
alias lens="/opt/lens/lens.AppImage &"
alias cntb="/opt/contabo/cntb"

#cloud
alias k=kubectl
alias h=helm
#alias lens="/opt/lens/OpenLens.AppImage --no-sandbox &"

# Reload zsh
alias szsh="source ~/.zshrc";

# NIX
# Reload home manager and zsh
alias reload="home-manager switch && source ~/.zshrc";
# Nix garbage collection
alias garbage="nix-collect-garbage -d && docker image prune --force";
# See which Nix packages are installed
alias installed="nix-env --query --installed";

#SSH
alias ssh-="ssh -p2222 ag@"
alias ssh-bastion="ssh -p2222 ag@194.163.181.218"
alias ssh-ctb1="ssh -p2222 ag@161.97.175.186"
alias ssh-ctb2="ssh -p2222 ag@194.163.128.218"
alias ssh-ctb3="ssh -p2222 ag@194.163.129.213"
alias ssh-ctb4="ssh -p2222 ag@194.163.130.106"

# SDKMAN
alias visualvm="$VISUALVM_HOME/bin/visualvm"
